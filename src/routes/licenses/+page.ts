import type { PageLoad } from './$types';

export const load: PageLoad = () => ({
	title: 'Licenses for third-party software',
	description: 'Licenses for third party software used on VideoSpeedup.com'
});
